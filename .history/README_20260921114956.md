# product-stock-managment-task
note:این پروژه در ابتدا با ریکت   راه‌اندازی شده بود. به دلیل محدودیت دسترسی به اینترنت پرسرعت برای ایجاد یک پروژه جدید و کمبود زمان، از همین پروژه به‌عنوان پایه استفاده کردم. به همین دلیل، برخی از بسته‌ها و کتابخانه‌های موجود در پروژه از قبل نصب و تنظیم شده‌اند و مربوط به ساختار اولیه پروژه هستند.

-run this command 
-php artisan migrate --seed

- in header of each  request please sent this parameter( this will identify whcih Tenant is currently used.)
X-Tenant-ID = 1 

- running the below command whcih check the stock  discrepancy

-php artisan inventory:reconcil {Tenant_id}

-Concurrency
Write a test that fires 100 concurrent out movements of quantity 1 against a product with
initial stock of 50 in the same warehouse.
Assert that exactly 50 succeed and 50 fail, and that final stock is 0
 php artisan test --filter=test_100_concurrent_out_movements_from_50_stock


- we have three endpoint here due to task each endpoint have it is own parameters
http://127.0.0.1:8000/api/stock-movements
body parameters
tenant_id, product_id, warehouse_id, type
ENUM('in','out','transfer'), quantity, reference
Use a meta JSON column on stock_movements to store
from_warehouse_id and to_warehouse_id for transfers

http://127.0.0.1:8000/api/stock-levels?warehouse_id=1
sent in parms the product_id and  warehouse_id for filter porpuse

http://127.0.0.1:8000/api/products/{SKU-4326}/history

